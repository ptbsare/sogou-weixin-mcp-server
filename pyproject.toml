
[project]
name = "sogou-weixin-mcp-server"
version = "0.0.1"
description = "MCP Server for Sogou WeChat Article Search using miku_ai"
dependencies = [
 "miku_ai",
 "mcp"
]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
